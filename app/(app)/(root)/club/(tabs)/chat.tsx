import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';

import { StyledText } from '@/components/ui';

const Chat = () => (
  <SafeAreaView>
    <StyledText>Chat club page</StyledText>
  </SafeAreaView>
);

export default Chat;
