import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';

import { StyledText } from '@/components/ui';

const Search = () => (
  <SafeAreaView>
    <StyledText size='sm'>Search page</StyledText>
  </SafeAreaView>
);

export default Search;
